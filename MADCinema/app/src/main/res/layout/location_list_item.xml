<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="5dip"
    android:paddingBottom="5dip">

    <TextView
        android:id="@+id/locationInformation"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="2"
        android:gravity="center_vertical"
        android:text="Loughborough"
        android:textAlignment="textStart"
        android:textColor="@android:color/black"
        android:textSize="24sp" />

    <ImageButton
        android:id="@+id/locationPreferenceButton"
        style="@android:style/Widget.DeviceDefault.ImageButton"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="6"
        android:background="@android:color/transparent"
        android:contentDescription="Pin Location"
        android:cropToPadding="false"
        android:scaleType="fitCenter"
        android:text="Button"
        app:srcCompat="@drawable/ic_location_on" />

    <ImageButton
        android:id="@+id/mapButton"
        style="@android:style/Widget.DeviceDefault.ImageButton"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="6"
        android:background="@android:color/transparent"
        android:contentDescription="Go to google maps"
        android:cropToPadding="false"
        android:scaleType="fitCenter"
        android:text="Button"
        app:srcCompat="@drawable/ic_map_black" />

</LinearLayout>